---
import ProfilePage from "../components/ProfilePage.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { content } from "../lib/site-data";

const lang = "en";
const copy = content[lang];
---

<BaseLayout lang={lang} title={`${copy.profile.title} · Heroes of Crypto`} description={copy.profile.subtitle}>
    <ProfilePage lang={lang} />
</BaseLayout>
